class Solution:
    def reverseWords(self, s: str) -> str:
        s=s.strip()
        s=s.split(" ")[::-1]
        result=[]
        for char in s:
            if(len(char)!=0):
               result.append(char)
        return " ".join(result)
